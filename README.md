# สภานักเรียน (Student Council Website)

เว็บไซต์สภานักเรียนที่ทำขึ้นเพื่อเป็นช่องทางติดต่อกับนักเรียนและการแบ่งปันข้อมูลข่าวสาร

## คุณสมบัติหลัก ✨

- **หน้าแรก (Home)**: แสดงข่าวสารประจำวัน ช่องทางติดต่อ และนำทางไปยังหน่วยงานต่างๆ
- **ฝ่ายวิชาการ (Academics)**: ข้อมูลเกี่ยวกับโครงการและกิจกรรมวิชาการ
- **ฝ่ายบัญชี (Accounting)**: ข้อมูลการเงิน งบประมาณ และรายการรายรับ-รายจ่าย
- **ฝ่ายกิจกรรม (Activities)**: ข้อมูลกิจกรรมประจำปี และการจัดการกิจกรรม
- **ติดต่อเรา (Contact)**: แบบฟอร์มติดต่อ และช่องทางการติดต่อพิเศษ
- **แชทกับแอดมิน (Chat)**: ระบบแชทแบบ real-time เพื่อติดต่อกับแอดมินเว็บทันที

## เทคโนโลยีที่ใช้ 🛠️

- **Next.js 16.1.6** - Framework React สำหรับ Server-Rendered Web Applications
- **React 19.2** - JavaScript Library สำหรับสร้าง User Interface
- **TypeScript** - ภาษา JavaScript ที่มี Type Safety
- **Tailwind CSS 4** - Utility-first CSS Framework
- **Turbopack** - Build System ที่รวดเร็ว

## วิธีการเรียกใช้ 🚀

### 1. ติดตั้ง Dependencies
```bash
npm install
```

### 2. รันเซิร์ฟเวอร์สำหรับพัฒนา
```bash
npm run dev
```

เปิดบราวเซอร์และไปที่ [http://localhost:3000](http://localhost:3000)

### 3. สำหรับ Production
```bash
# สร้าง build ที่ปรับให้เหมาะสม
npm run build

# รันเซิร์ฟเวอร์ production
npm start
```

## โครงสร้างโปรเจค 📁

```
student-council-web/
├── src/
│   ├── app/
│   │   ├── academics/          # หน้าฝ่ายวิชาการ
│   │   ├── accounting/         # หน้าฝ่ายบัญชี
│   │   ├── activities/         # หน้าฝ่ายกิจกรรม
│   │   ├── contact/            # หน้าติดต่อเรา
│   │   ├── layout.tsx          # Layout หลัก (Navigation + Chat)
│   │   ├── page.tsx            # หน้าแรก
│   │   └── globals.css         # CSS ทั่วไป
│   └── components/
│       ├── Navigation.tsx       # Component สำหรับเมนูนำทาง
│       └── ChatWidget.tsx       # Component สำหรับแชทกับแอดมิน
├── public/                      # ไฟล์เก็บเซ็ท (images, icons)
├── package.json
├── next.config.ts              # ไฟล์ Configuration ของ Next.js
├── tailwind.config.ts          # ไฟล์ Configuration ของ Tailwind CSS
└── tsconfig.json               # ไฟล์ Configuration ของ TypeScript
```

## หน้าต่างๆของเว็บไซต์ 📄

| หน้า | URL | คำอธิบาย |
|-----|-----|---------|
| หน้าแรก | `/` | แสดงข่าวสาร ช่องทางติดต่อ และนำทางไปยังหน่วยงาน |
| ฝ่ายวิชาการ | `/academics` | ข้อมูลและการติดต่อฝ่ายวิชาการ |
| ฝ่ายบัญชี | `/accounting` | ข้อมูลการเงินและการติดต่อฝ่ายบัญชี |
| ฝ่ายกิจกรรม | `/activities` | กิจกรรมประจำปีและข้อมูลการจัดการกิจกรรม |
| ติดต่อเรา | `/contact` | แบบฟอร์มติดต่อและช่องทางการติดต่อ |

## Features ของ Chat Widget 💬

- ✅ Open/Close แบบ Toggle
- ✅ ส่งและรับข้อความ Real-time
- ✅ แสดงเวลาของข้อความ
- ✅ Responsive Design (Mobile-friendly)
- ✅ Auto-scroll ไปยังข้อความล่าสุด
- ✅ Notification เมื่อได้รับข้อความใหม่

## การเปลี่ยนแปลง/ปรับแต่ง 🎨

### เปลี่ยนชื่อหรือข้อมูล
1. ไปที่ไฟล์ `src/app/page.tsx` เพื่อเปลี่ยนข้อมูลหน้าแรก
2. ไปที่ไฟล์ `src/components/Navigation.tsx` เพื่อเปลี่ยนสีหรือเมนูนำทาง
3. ไปที่ไฟล์เพจแต่ละหน้าเพื่อปรับปรุงข้อมูล

### เปลี่ยนสีและ Style
ใช้ Tailwind CSS Classes ใน JSX/TSX ไฟล์ต่างๆ เช่น:
- `bg-blue-600` = สีพื้นหลังสีน้ำเงิน
- `text-white` = สีตัวอักษรสีขาว
- `hover:shadow-lg` = แสดงเงาเมื่อ Hover

## Deployment 🌐

### Deploy บน Vercel
1. Push code ไปยัง GitHub
2. Connect GitHub Repository กับ Vercel
3. Vercel จะ Auto-deploy เมื่อมีการ push ใหม่

### Deploy บน Server อื่นๆ
```bash
npm run build
npm start
```

## ติดต่ออื่นๆ 📞

หากมีข้อเสนอแนะหรือต้องการปรับปรุงเพิ่มเติม สามารถติดต่อผ่าน:
- 📧 Email: student.council@school.ac.th
- 📱 Phone: 02-XXXX-XXXX
- 💬 Chat: ใช้ระบบแชทบนเว็บไซต์

---

**สร้างสรรค์ด้วยใจ เพื่อทุกคนในสภานักเรียน ❤️**
